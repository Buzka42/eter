# Eter Flutter application

Run commands from this directory. Project-level status, architecture, planning,
Firebase, and release instructions live in `../docs/`.

Quick verification:

```powershell
flutter pub get
flutter test
flutter analyze lib test
```

Configured builds use `tool/run-configured.ps1`; the populated helper is ignored
and must not be committed.

